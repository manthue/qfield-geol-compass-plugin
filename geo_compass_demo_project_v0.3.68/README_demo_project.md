# Geo Compass Demo Project v0.3.68

Open `geo_compass_demo.qgs` in QField. The project-specific plugin file is `geo_compass_demo.qml`.

Measurements are appended as GeoJSON point features to `geology_measurements.geojson` in this folder. This demo project intentionally does not include a measurement GeoPackage.
