# Geo Compass Demo Project v0.3.69

Open `geo_compass_demo.qgs` in QField. The project-specific plugin file is `geo_compass_demo.qml`.

The project legend includes `geology_measurements`, sourced from `geology_measurements.geojson`. Measurements are appended as GeoJSON point features, and the plugin reloads/repaints the layer after each save.
